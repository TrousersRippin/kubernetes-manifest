---
apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: arrr
  labels:
    app: sonarr
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8989
      targetPort: 8989
      nodePort: 30089
      name: http
  selector:
    app: sonarr
